/**
 * @deprecated Re-export shim for legacy imports. New code should import from
 * `./types/pedigree.types` directly.
 */
export type { Individual, PedigreeSummary, ReservedColumn } from './types/pedigree.types';
export { RESERVED_COLUMNS } from './types/pedigree.types';
